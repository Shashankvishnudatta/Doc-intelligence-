# Frontend

This folder contains the Next.js frontend for the Document Intelligence RAG platform.

Main pages:

* `/` - landing / overview
* `/upload` - document upload and processing
* `/documents` - knowledge base
* `/chat` - selected-document RAG chat

Run locally:

```powershell
npm install
npm run dev
```

The main setup instructions are in the root `README.md`.
